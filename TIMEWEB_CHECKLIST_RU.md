# ✅ Чеклист для Timeweb App Platform

## 🎯 Проблема: Изменения не отображаются на сайте

### Причины:
1. Timeweb не запустил новый деплой автоматически
2. Переменные окружения не настроены правильно
3. Кэш браузера показывает старую версию

---

## 📋 Что нужно проверить на Timeweb

### 1️⃣ Проверить деплой

Зайдите в панель Timeweb → Ваше приложение → Вкладка **"Деплои"**

**Проверьте:**
- ✅ Последний деплой показывает коммит `174a666` (feat: обновить favicon)
- ✅ Статус деплоя: **"Успешно"** (зеленая галочка)
- ✅ Дата/время деплоя - сегодня

**Если деплоя нет или статус "В процессе":**
- Подождите 5-10 минут
- Обновите страницу панели Timeweb
- Проверьте логи сборки

**Если деплой завершился с ошибкой:**
- Откройте логи сборки
- Найдите красный текст с ошибкой
- Отправьте мне ошибку для исправления

---

### 2️⃣ Проверить переменные окружения

Зайдите в панель Timeweb → Ваше приложение → Вкладка **"Переменные окружения"**

**ОБЯЗАТЕЛЬНЫЕ переменные:**

```bash
# 1. База данных (проверьте, что URL правильный)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# 2. NextAuth (КРИТИЧЕСКИ ВАЖНО!)
NEXTAUTH_URL=https://miss-kurochka.com
NEXTAUTH_SECRET=ваш-секретный-ключ-минимум-32-символа
AUTH_TRUST_HOST=true

# 3. Public URL для клиента
NEXT_PUBLIC_APP_URL=https://miss-kurochka.com

# 4. Google OAuth (для входа через Google)
GOOGLE_CLIENT_ID=782418180516-44cj4a3d9j6ab4oqlvhfmub3c74c2m3j.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=ваш-client-secret-из-google-console

# 5. OpenAI (для ИИ-помощника)
OPEN_AI=sk-proj-ваш-ключ

# 6. Email (для восстановления пароля, опционально)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш-email@gmail.com
SMTP_PASSWORD=ваш-app-password
SMTP_FROM=Miss Kurochka <noreply@miss-kurochka.com>

# 7. Finik Pay (опционально, если включена оплата)
FINIK_ENV=beta
FINIK_API_KEY=ваш-ключ
FINIK_ACCOUNT_ID=ваш-аккаунт-id
```

**❗ ВАЖНО:**
- Все URL должны быть с **HTTPS** (не HTTP!)
- `NEXTAUTH_URL` должен быть **ТОЧНО** `https://miss-kurochka.com`
- `AUTH_TRUST_HOST=true` **ОБЯЗАТЕЛЬНО** для работы NextAuth
- Не должно быть пробелов вокруг `=`

---

### 3️⃣ Принудительно запустить новый деплой

Если последний деплой не содержит ваши изменения:

**Вариант 1: Через панель Timeweb**
1. Перейдите в раздел **"Деплои"**
2. Нажмите кнопку **"Новый деплой"**
3. Выберите ветку **`main`**
4. Нажмите **"Запустить"**
5. Дождитесь завершения (5-10 минут)

**Вариант 2: Через пустой коммит**
```bash
git commit --allow-empty -m "trigger: force redeploy"
git push origin main
```

---

### 4️⃣ Проверить домен

Зайдите в панель Timeweb → Ваше приложение → Вкладка **"Домены"**

**Проверьте:**
- ✅ Домен `miss-kurochka.com` добавлен
- ✅ SSL-сертификат активен (зеленый замок)
- ✅ Автоматическое перенаправление с HTTP на HTTPS включено

**Если домен не работает:**
1. Проверьте DNS записи (A-запись на IP Timeweb)
2. Подождите до 24 часов для распространения DNS
3. Проверьте SSL-сертификат (должен быть Let's Encrypt)

---

### 5️⃣ Очистить кэш браузера

Даже если деплой успешный, браузер может показывать старую версию.

**Как очистить кэш:**

**Chrome / Edge:**
1. Откройте сайт `https://miss-kurochka.com`
2. Нажмите `Ctrl + Shift + R` (Windows) или `Cmd + Shift + R` (Mac)
3. Или: `F12` → Консоль → правый клик на кнопку обновления → "Очистить кэш и жесткая перезагрузка"

**Firefox:**
1. Откройте сайт
2. Нажмите `Ctrl + Shift + Delete`
3. Выберите "Кэш" → Удалить

**Safari:**
1. Safari → Настройки → Дополнения → Показать меню "Разработка"
2. Разработка → Очистить кэш

---

### 6️⃣ Проверить логи приложения

Если сайт не работает или показывает ошибку:

**Зайдите в панель Timeweb → Ваше приложение → Вкладка "Логи"**

**Что искать:**
- ❌ `[auth][error] UntrustedHost` - не установлен `AUTH_TRUST_HOST=true`
- ❌ `Error: connect ECONNREFUSED` - не работает база данных
- ❌ `Error: Invalid environment variable` - не установлены переменные окружения
- ❌ `Error: Module not found` - ошибка сборки, нужен новый деплой

**Если видите ошибки:**
- Скопируйте полный текст ошибки (5-10 строк)
- Отправьте мне для исправления

---

## 🔄 Пошаговая проверка (по порядку)

### Шаг 1: Проверить последний деплой
```
Timeweb → Приложение → Деплои
Статус последнего деплоя: ✅ Успешно?
Коммит: 174a666 или новее?
```

### Шаг 2: Проверить переменные
```
Timeweb → Приложение → Переменные окружения
✅ NEXTAUTH_URL=https://miss-kurochka.com
✅ AUTH_TRUST_HOST=true
✅ NEXT_PUBLIC_APP_URL=https://miss-kurochka.com
✅ DATABASE_URL=postgresql://...
```

### Шаг 3: Проверить домен
```
Timeweb → Приложение → Домены
✅ miss-kurochka.com - активен
✅ SSL - активен (зеленый замок)
```

### Шаг 4: Открыть сайт
```
1. Откройте https://miss-kurochka.com
2. Нажмите Ctrl + Shift + R (жесткая перезагрузка)
3. Проверьте, отображаются ли изменения
```

### Шаг 5: Если не работает - проверить логи
```
Timeweb → Приложение → Логи
Ищите красный текст с ошибками
```

---

## 🚨 Частые проблемы

### Проблема 1: Белая страница на сайте
**Причина:** Ошибка JavaScript или проблема с NextAuth

**Решение:**
1. Откройте браузерную консоль (F12 → Console)
2. Проверьте логи Timeweb на ошибки
3. Убедитесь, что `AUTH_TRUST_HOST=true` установлен

---

### Проблема 2: "UntrustedHost" ошибка в логах
**Причина:** Не установлена переменная `AUTH_TRUST_HOST=true`

**Решение:**
1. Timeweb → Переменные окружения
2. Добавьте: `AUTH_TRUST_HOST=true`
3. Сохраните и перезапустите приложение

---

### Проблема 3: Изменения не отображаются
**Причина:** Кэш браузера или старый деплой

**Решение:**
1. Проверьте дату последнего деплоя в Timeweb
2. Очистите кэш: `Ctrl + Shift + R`
3. Попробуйте в режиме инкогнито

---

### Проблема 4: "Cannot connect to database"
**Причина:** Неправильный `DATABASE_URL` или база не запущена

**Решение:**
1. Проверьте `DATABASE_URL` в переменных окружения
2. Проверьте, что база данных запущена (Timeweb → Базы данных)
3. Проверьте формат: `postgresql://user:password@host:5432/dbname`

---

## 📞 Если ничего не помогло

**Отправьте мне следующую информацию:**

1. **Скриншот страницы деплоев** (Timeweb → Деплои)
2. **Скриншот переменных окружения** (замажьте секреты!)
3. **Последние 20 строк логов** (Timeweb → Логи)
4. **Скриншот консоли браузера** (F12 → Console)
5. **Что вы видите на сайте** (скриншот)

---

## ✅ Успешный деплой выглядит так:

1. ✅ Последний деплой: статус "Успешно", коммит `174a666`
2. ✅ Переменные: `NEXTAUTH_URL=https://miss-kurochka.com`, `AUTH_TRUST_HOST=true`
3. ✅ Домен: `miss-kurochka.com` с SSL
4. ✅ Сайт открывается без ошибок
5. ✅ Логотип обновился
6. ✅ Все страницы работают (главная, политика конфиденциальности, условия)
7. ✅ Вход через Google работает
8. ✅ Мобильная навигация скрыта для неавторизованных пользователей

---

**Дата создания:** 3 июня 2026  
**Последнее обновление:** 3 июня 2026  
**Версия:** 1.0
